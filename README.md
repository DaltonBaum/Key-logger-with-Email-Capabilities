# Key-logger-with-Email-Capabilities
A typical key logger that saves to a text file and emails back the information logged.


!!!you need to install pynput with the "pip3 install pynput" command!!!

run in a terminal
